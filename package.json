{
  "scripts": {
    "test": "npm run build && jasmine-node _out/ui/ --junitreport",
    "start": "npm run build && node _out/server.js",
    "build": "npm run fix && tsc && node _out/build.js",
    "postinstall": "typings install",
    "fix": "tsfmt -r"
  },
  "dependencies": {
    "express": "^4.13.4",
    "material-ui": "^0.14.4",
    "react": "0.14.7",
    "react-dom": "0.14.7",
    "react-tap-event-plugin": "0.2.2",
    "typescript": "^1.8.7",
    "typescript-formatter": "^2.1.0"
  },
  "devDependencies": {
    "browserify": "^13.0.0",
    "jasmine-core": "^2.4.1",
    "jasmine-node": "^1.14.5",
    "typescript": "^1.7.5",
    "typescript-formatter": "^2.0.0",
    "typings": "^0.6.8"
  },
  "engines": {
    "node": "^5.6"
  }
}
