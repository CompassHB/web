{
  "scripts": {
    "test": "npm run compile && jasmine-node ui/ --junitreport",
    "start": "npm run compile && node server.js",
    "compile": "tsc",
    "postinstall": "typings install",
    "fix": "tsfmt -r"
  },
  "dependencies": {
    "express": "^3.2.6",
    "react": "^0.14.7",
    "react-bootstrap": "^0.28.3",
    "react-dom": "^0.14.7",
    "wpcom": "^4.8.3"
  },
  "devDependencies": {
    "jasmine-core": "^2.4.1",
    "jasmine-node": "^1.14.5",
    "typescript": "^1.7.5",
    "typescript-formatter": "^2.0.0",
    "typings": "^0.6.8"
  },
  "engines": {
    "node": "^5.6"
  }
}
